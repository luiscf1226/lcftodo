/** Where signed-in users without a team (Clerk "pending" session) create or join one. */
export const ONBOARDING_PATH = "/onboarding";

/** Calendar of personal tasks that aren't in a project yet. */
export const INBOX_PATH = "/app/inbox";

/** Home of the signed-in app (Today). */
export const APP_HOME_PATH = "/app";
